<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
        <title>{{titulo}}</title>
    </head>
    <body>
            <div class="container">
                <div class="row justify-content-sm-center">
                    <div class="col-sm-5">
                        <img src="img/hello.jpeg" class="img-fluid img-thumbnail" alt="Responsive image">
                    </div>  
                </div>
            </div>
            <h1 class="text-light bg-{{color}} text-center">{{saludo}}</h1>  
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item active">
                            <a class="nav-link" href="/">Inicio</a>
                        </li>
                        {{#if coordinador}}
                        <li class="nav-item active">
                            <a class="nav-link" href="/crear">Crear curso</a>
                        </li>
                        {{/if}}
                        {{#if aspirante}}
                        <li class="nav-item active">
                            <a class="nav-link" href="/cursos">Cursos</a>
                        </li>
                        <li class="nav-item active">
                            <a class="nav-link" href="/inscribir">Inscribir</a>
                        </li>
                        {{/if}}
                        {{#if coordinador}}
                        <li class="nav-item active">
                            <a class="nav-link" href="/verCursos">Ver Cursos</a>
                        </li>
                        <li class="nav-item active">
                            <a class="nav-link" href="/eliminar">Eliminar Estudiante</a>
                        </li>
                        {{/if}}
                        </ul>
                    {{#if session}}
                    <form action="/salir" method="get" class="form-inline my-2 my-lg-0">
                        <h3 class="text-success mr-sm-4">Bienvenido {{nombres}}</h3>
                        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Salir</button>
                    </form> 
                    {{else}}
                    <form action="/autenticacion" method="post" class="form-inline my-2 my-lg-0">
                        <input name="Nombre" class="form-control mr-sm-2" type="text" placeholder="Nombre" >
                        <input name="Contraseña" class="form-control mr-sm-2" type="password" placeholder="Contraseña">
                        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Ingresar</button>
                    </form>
                    {{/if}}
                </div>
            </nav>
